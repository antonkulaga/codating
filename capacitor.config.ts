import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.github.antonkulaga',
  appName: 'codating',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
